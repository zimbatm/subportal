[package]
name = "notify-send"
version = "0.1.0"
edition = "2021"
description = "Drop-in notify-send replacement — forwards to subportal"

[dependencies]
subportal-lib = { workspace = true }
tokio = { workspace = true, features = ["rt-multi-thread"] }
anyhow = { workspace = true }
